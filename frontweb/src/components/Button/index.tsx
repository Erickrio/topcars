import './styles.css';

const Button = () => {
  return (
    <div className="btn-container">
        <button className="btn btn-primary">
          <h6>ver catalogo</h6>
        </button>
    </div>
  );
};

export default Button;
